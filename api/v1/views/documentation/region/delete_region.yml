    Deletes a region based on the id provided
    ---
    tags:
      - States
    parameters:
      - name: region_id
        in: path
        type: string
        required: true
        description: The uuid of the region to delete
    responses:
      404:
        description: State not found!
      200:
        description: Deletion request sucessful
